@model IEnumerable<NEWWORK.Tbl_iş_Durumu>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.iş_onay_durumu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.iş_onay_durumu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iş_Durum_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.iş_Durum_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iş_Durum_ID })
        </td>
    </tr>
}

</table>
