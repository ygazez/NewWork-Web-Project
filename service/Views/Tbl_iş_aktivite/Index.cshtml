@model IEnumerable<NEWWORK.Tbl_iş_aktivite>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.iş_aktivite_durumu)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.iş_aktivite_durumu)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.iş_aktivite_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.iş_aktivite_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.iş_aktivite_ID })
        </td>
    </tr>
}

</table>
